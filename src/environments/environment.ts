export const environment = {
  production: false,
  /** URL base del API. El equipo backend debe apuntar esto a su gateway/servicio real. */
  apiBaseUrl: '/api',
  /** true = los servicios usan datos simulados en memoria (sin backend). */
  useMocks: true,
};
