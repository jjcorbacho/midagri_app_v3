## Sigue haciendo falta, pero ya no por lucide (retirado): @angular/animations@22.1
## pide @angular/core@22.1.0 exacto y el resto del framework está en ^22.0.
## Se resuelve alineando @angular/core a ^22.1.0; hasta entonces, peers heredados.
legacy-peer-deps=true
